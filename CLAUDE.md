# CLAUDE.md - overload-party-infra

## 設計思想
- エラーは握りつぶさず根本解決する。ログだけして握りつぶすのは禁止
- デフォルト値へのフォールバックを行わない。意図しない値になるならエラーにする
- 一つの関数に複数の責務を負わせない

## コーディング方針
- 関数などには docs コメントを付与する
- 実装との二重管理になるので What コメントを書いてはいけない
- 実装から読み取れない意図は Why コメントとして記述する。
- `variable` ブロックで `default` を一切使わない（環境ごとの値は `environments/<env>/` の呼び出し側で必ず明示指定する）
  - 理由: `default` があるとフォールバックで意図しない値が無言のまま適用される。必須指定を強制することで「どの環境が何を渡しているか」を常に明示する
- `output` は必要なもの以外作らない
  - 理由: 使われない output を増やすと、本当に参照されている値が埋もれて把握しにくくなり、後の削除コストも膨らむ
- state に平文で機密情報が残ることを避けるため、Terraform から Secret Manager のバージョンの値を投入しない

## 禁止事項
- Google CloudのことをGCPと表現してはいけない。（GCPは旧名称のため）
- このファイル（CLAUDE.md）を Claude が書き換えない。
  ルールの追加・修正は人間が明示的に指示した場合のみ行う
