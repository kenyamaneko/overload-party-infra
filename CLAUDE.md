# CLAUDE.md - overload-party-infra

## 行動制約

- エラーは握りつぶさない
- コメントは意図（なぜそうしたか）が読み取りづらい場合のみ記述する
- `variable` ブロックで `default` を一切使わない（環境ごとの値は `environments/<env>/` の呼び出し側で必ず明示指定する）
  - 理由: `default` があるとフォールバックで意図しない値が無言のまま適用される。必須指定を強制することで「どの環境が何を渡しているか」を常に明示する
- `output` は必要なもの以外作らない
  - 理由: 使われない output を増やすと、本当に参照されている値が埋もれて把握しにくくなり、後の削除コストも膨らむ
  - 新規追加時は「どこが consumer か（他リポ / CI / 手動オペ）」を明確にしてから。使う当てがないなら追加しない
